framework:
    secret: '%env(APP_SECRET)%'
    http_method_override: false
    handle_all_throwables: true

    session:
        enabled: true
        cookie_secure: auto
        cookie_samesite: lax

    form: true

    php_errors:
        log: true
