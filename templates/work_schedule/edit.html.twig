{% extends 'base.html.twig' %}

{% block title %}{{ 'common.edit'|trans }} {{ schedule.name }}{% endblock %}

{% block body %}
    <div class="page-header">
        <h1>{{ 'common.edit'|trans }} {{ schedule.name }}</h1>
    </div>

    <div class="card">
        <div class="card__body">
            {% include 'work_schedule/_form.html.twig' %}
        </div>
    </div>
{% endblock %}
