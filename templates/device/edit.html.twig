{% extends 'base.html.twig' %}

{% block title %}{{ 'common.edit'|trans }} {{ device.name }}{% endblock %}

{% block body %}
    <div class="page-header">
        <h1>{{ 'common.edit'|trans }} {{ device.name }}</h1>
    </div>

    <div class="card">
        <div class="card__body">
            {% include 'device/_form.html.twig' %}
        </div>
    </div>
{% endblock %}
