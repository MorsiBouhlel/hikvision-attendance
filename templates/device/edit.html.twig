{% extends 'base.html.twig' %}

{% block title %}Éditer {{ device.name }}{% endblock %}

{% block body %}
    <div class="page-header">
        <h1>Éditer {{ device.name }}</h1>
    </div>

    <div class="card">
        <div class="card__body">
            {% include 'device/_form.html.twig' %}
        </div>
    </div>
{% endblock %}
