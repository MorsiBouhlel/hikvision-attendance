{% extends 'base.html.twig' %}

{% block title %}Éditer {{ department.name }}{% endblock %}

{% block body %}
    <div class="page-header">
        <h1>Éditer {{ department.name }}</h1>
    </div>

    <div class="card">
        <div class="card__body">
            {% include 'department/_form.html.twig' %}
        </div>
    </div>
{% endblock %}
