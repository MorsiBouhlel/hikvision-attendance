{% extends 'base.html.twig' %}

{% block title %}{{ 'common.edit'|trans }} {{ department.name }}{% endblock %}

{% block body %}
    <div class="page-header">
        <h1>{{ 'common.edit'|trans }} {{ department.name }}</h1>
    </div>

    <div class="card">
        <div class="card__body">
            {% include 'department/_form.html.twig' %}
        </div>
    </div>
{% endblock %}
