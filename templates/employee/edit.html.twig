{% extends 'base.html.twig' %}

{% block title %}{{ 'common.edit'|trans }} {{ employee.fullName }}{% endblock %}

{% block body %}
    <div class="page-header">
        <h1>{{ 'common.edit'|trans }} {{ employee.fullName }}</h1>
    </div>

    <div class="card">
        <div class="card__body">
            {% include 'employee/_form.html.twig' %}
        </div>
    </div>
{% endblock %}
